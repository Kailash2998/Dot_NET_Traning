@model ForgenKeyRelationship.NetCore.Models.Publisher

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <h1 class="display-4">Publisher Delete</h1>
    <h3 class="text-center">Are you sure you want to delete this?</h3>
    <hr />

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title text-center text-color-primary">Publisher Information</h4>
                        <hr />
                        <dl class="row">
                            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Name)</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.Name)</dd>

                            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Address)</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.Address)</dd>
                        </dl>

                        <form asp-action="Delete">
                            <input type="hidden" asp-for="PublisherId" />
                            <input type="submit" value="Delete" class="btn btn-danger" /> |
                            <a asp-action="GetPublisherList" class="btn btn-secondary">Back to List</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
